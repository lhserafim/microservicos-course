# Nome do servidor de aplicação, o mesmo que está no application.properties
spring.application.name=hr-worker
# localização do servidor de configuração na porta padrão
spring.cloud.config.uri=http://localhost:8888

spring.profiles.active=test